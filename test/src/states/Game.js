/*
 * Game state
 * ==========
 *
 * A sample Game state, displaying the Phaser logo.
 */

import * as a from 'phaser-ui';

export default class Game extends Phaser.State {

  create() {
    console.log(a)
  }

}
